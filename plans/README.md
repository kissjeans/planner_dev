# Implementation Plans

Generated by the `improve` skill on 2026-06-11, against commit `442c9a4`.
Source of findings: a direct code audit cross-checked against
`docs/qa-report-2026-06-11.md` (every finding re-verified against the live
working tree).

> **⚠️ Working tree was dirty when these plans were written.** Several in-scope
> files had uncommitted modifications vs. `442c9a4`
> (`web/routes/auth.py`, `infra/llm/basic.py`, `bot/handlers/task_router.py`,
> `domain/intent.py`, `web/routes/team.py`) and `web/routes/board.py` was
> untracked. A `git diff 442c9a4..HEAD` therefore does NOT reflect what the
> executor will see. **Drift detection for every plan is: open the cited files
> and compare the "Current state" excerpts against the live code.** Do not rely
> on the SHA diff. Consider committing the working tree before executing.

Each executor: read the plan fully before starting, honor its STOP conditions,
and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Fail startup on default JWT secret in production | P1 | S | — | DONE (`fc63459`) |
| 002 | Restrict `/dev-login` to loopback callers | P1 | S | — | DONE (`c7c018f`) |
| 003 | Require a known sender before processing messages | P1 | M | — | DONE (`94a3c4c`) |
| 004 | Per-user rate-limit (throttle) middleware | P2 | M | — | DONE (`1f32ba8`) |
| 005 | Return 400/422 (not 500) for malformed web input | P2 | M | — | DONE (`edb8f3a`) |
| 006 | Stop regex parser crashing on out-of-range dates | P2 | S | — | DONE (`7fe8517`) |
| 007 | Surface 404 for vacation on unknown person | P2 | S | 005 | DONE (`f4732d1`) |
| 008 | Route "delete project X" to drop, not create | P3 | S | 006 | DONE (`b003b82`) |
| 009 | Record real actor id on audit entries | P3 | S | 005 | DONE (`047bb29`) |
| 010 | Harden voice handler (no assert, size cap) | P3 | S | — | DONE (`559bcb1`) |
| 011 | Atomic plan-status transition on confirm (TOCTOU) | P1 | S | — | DONE (`282f751`) |
| 012 | Edit loop: supersede old proposal, cancel orphan project | P1 | M | 011 | DONE (`df14ec3`) |
| 013 | Wire isdayoff.ru into startup + yearly refresh job | P2 | M | — | DONE (`771b02c`) |
| 014 | AddProject: solve before persisting (no orphan rows) | P2 | M | — | DONE (`de9681d`) |
| 015 | Anthropic client timeout/retries + explain fallback | P2 | S | — | DONE (`1e7757e`) |
| 016 | Persist template tasks to tasks table (web visibility) | P2 | M | 014 | DONE (`a377f23`) |
| 017 | Migrate python-jose → PyJWT | P3 | S | — | DONE (`3c19599`) |
| 018 | Gate + validate `capture_task` (write-gate bypass) | P1 | S | — | DONE (2026-06-15; capture → admin-only per decision) |
| 019 | Load board: drop round-before-ceil (true slot load) | P1 | S | — | DONE (2026-06-15) |
| 020 | Board reassign: validate ids, single people query | P2 | S | — | DONE (2026-06-15; `test_reassign_non_uuid_returns_400` present & green) |
| 021 | Graceful shutdown + scheduler job error handling | P2 | M | — | DONE (2026-06-15) |
| 022 | `/whatif`: faithful baseline (real deps + durations) | P2 | M | — | DONE (2026-06-16; `list_task_dependencies` repo method + `_base_request` now threads real `Dependency` edges + task names) |
| 023 | Capability coverage invariant stays in [0,1] | P2 | S | — | DONE (2026-06-15) |
| 024 | Guard matplotlib figure leak (try/finally close) | P3 | S | — | DONE (2026-06-15) |
| 025 | Seed: scope capability rebuild (no global wipe) | P3 | S–M | — | DONE (2026-06-16; idempotency integration test added & green — re-run stable + manual non-seed link survives) |

**Executed 2026-06-11** by dispatched executors on worktree branch
`advisor/batch-001-017` (worktree: `../planner_dev-advisor`, baseline
`dfeede1` = snapshot of the dirty tree). All 17 reviewed and APPROVED.
Final gate on the branch: 404 unit tests pass, `ruff` clean,
`mypy src/planner --strict` clean (78 files).

> **✅ INTEGRATED INTO `main` on 2026-06-15** via the patch path
> (`git apply` of `dfeede1..3c19599`). WIP snapshotted first at `7b9af2e`, the
> 17 fixes committed at `f13f8e8` on top of `442c9a4`. Post-integration gates
> on `main`: `ruff check src tests` clean, `mypy src/planner --strict` clean
> (78 files), `uv run pytest tests/unit` = 404 passed. `uv sync` swapped
> python-jose → pyjwt (plan 017). Not pushed. (10 `ruff check .` findings remain
> in `alembic/versions/*` template code — outside the `src tests` gate, not from
> this batch.)

Original (pre-integration) recipe, for reference:
commit/stash your working tree, then
`git -C ../planner_dev-advisor diff dfeede1..HEAD | git apply` (or
cherry-pick `fc63459..3c19599`), then `uv sync --extra dev` (017 changed
pyproject/uv.lock). Caveats: real-Postgres paths (011/012/014/016 repo
methods) verified by unit fakes + mypy only — integration suite needs
Docker; executor noted PyJWT warns on short test secrets (cosmetic).
Plan-013 doc gap found by executor: its test-placement instruction would
have left one assertion outside the patch scope (fixed during execution).

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) |
REJECTED (one-line rationale).

Plans 011–017 were added by a second audit round on 2026-06-11 (same commit,
same dirty working tree — the drift warning above applies to them equally).
Round 2 covered what round 1 did not: solver core, `infra/db/repo.py`,
use-cases, LLM/calendar adapters, scheduler, migrations, dependencies. Not
audited in either round: scheduler job failure semantics under APScheduler,
`asyncio.gather` shutdown behavior in `main.py`, `app/render/*`,
`what_if.py` internals, seed loader.

Plans 018–025 were added by a **third audit round on 2026-06-15** (same commit
`442c9a4`, same dirty working tree — the drift warning above applies equally; in
fact all of round 3's target files are uncommitted/untracked, so the
open-the-file excerpt comparison is the ONLY reliable drift check). Round 3 was
a *reconcile* run: it deliberately skipped everything rounds 1–2 already planned
(001–017) and audited **only** the post-audit feature code and the modules
rounds 1–2 listed as un-audited. Coverage: `app/capture_task.py`,
`app/admin_board.py`, `domain/slots.py`, `web/routes/board.py`,
`domain/capability.py` + `app/suggest_assignees.py`, `main.py` shutdown,
`infra/scheduler.py`, `app/render/*`, `bot/handlers/whatif.py`, `seed/load_seed.py`.
Each finding was re-read and confirmed against the live working tree by the
advisor before planning. **Not audited in round 3** (deliberately, as already
covered by 001–017 or out of the new surface): the capability ORM models,
migration `0003_project_priority.py` internals beyond its existence,
`app/load_summary.py`, the bot `suggest`/`load` handlers' formatting.

## Recommended order

Security first (001 → 002 → 003 → 004), then correctness (005 → 006 → 007 →
008 → 009 → 010). 001 and 002 are the must-fix admin-bypass pair and are tiny;
do them first. Round-2 plans: 011 → 012 (plan-integrity pair, do these right
after 004), then 014 → 016, then 013 / 015 / 017 in any order.

Round-3 plans (independent of each other — no hard ordering between them; run by
priority): **018** (security: capture write-gate) and **019** (load-board math)
first, then **020 / 021 / 022 / 023**, then **024 / 025**. None of the round-3
plans depend on a round-1/2 plan to *execute*, but two have a logical relation
worth knowing (see Dependency notes): 020 is the same bug class as DONE-005 on a
newer endpoint, and 022 is adjacent to DONE-012.

## Dependency notes

- **007 depends on 005** — both edit `web/routes/team.py::add_vacation`. Run
  005 (input validation) before 007 (remove the error-suppress) so the second
  reads the file the first left.
- **008 depends on 006** — both edit `infra/llm/basic.py`. Run 006
  (`_parse_date` hardening) before 008 (delete-phrasing classification).
- **009 coordinates with 005 and 007** — 009 touches `web/routes/board.py`
  (shared with 005) and `web/routes/team.py::_actor` (file shared with 005/007).
  Run after 005 and 007. Each plan's drift check (excerpt comparison) will catch
  any conflict.
- **003 and 010 both edit `bot/handlers/task_router.py`** but different
  functions (`_handle_text` vs `handle_voice`). No hard dependency; run in
  numeric order and rely on the drift check.
- **003 and 004 together** form the original QA "H1" finding: 003 is the
  allowlist half (block unknown senders), 004 is the throttle half (rate-limit
  known senders). They touch different code and can run in either order.
- **012 depends on 011** — 012 uses the `transition_plan_status` repo method
  that 011 introduces.
- **016 depends on 014** — both rewrite `AddProjectUseCase.execute`; 016
  assumes 014's compute-first ordering and inserts its persistence call there.
- **011/012/014/016 all touch `tests/unit/app/conftest.py::FakeRepo`** and
  `repo.py`/`ports.py` — execute in numeric order; each plan's excerpt-based
  drift check catches the predecessor's changes.
- **010 and 012 both edit `bot/handlers/task_router.py`** (different
  functions: `handle_voice` vs `_handle_text`/`handle_edit_text`).
- **018 also edits `bot/handlers/task_router.py`** (reorders the
  `CaptureTaskIntent` branch relative to the `can_execute` gate) and
  `domain/intent.py` (`WRITE_KINDS`) — files touched by DONE 003/010/012. No hard
  dependency; if the round-1/2 batch is integrated first, run 018's excerpt drift
  check before editing.
- **020 is the same bug class as DONE-005** (malformed web input → 500) but on
  the newer `web/routes/board.py::reassign` endpoint that 005 did not cover. No
  execution dependency; it stands alone.
- **022 is adjacent to DONE-012**: 012 makes confirm *supersede* prior committed
  plan versions. Once 012 is integrated, `list_committed_plans` should return one
  committed version per project — which also resolves the round-3 reconcile note
  below about `_committed_load` double-counting. 022 itself (faithful `/whatif`
  baseline) is independent and can run before or after 012.
- **019 and 020 both touch the admin-board surface** but different layers (019 =
  `app/admin_board.py` pure math + `domain/slots.py`; 020 = `web/routes/board.py`
  route). No overlap; run in either order.

## Notes on scope split

The QA report's **H1** ("no rate-limit + no allowlist") was split into two
plans because the fixes live in different places and have different risk:
- allowlist / known-sender gate → **plan 003** (`_handle_text`)
- throttle / rate-limit → **plan 004** (new middleware + `runner.py`)

## Findings considered and rejected (or deferred — not planned this round)

These were in the audit but not turned into plans now. Recorded so they are not
re-audited blindly.

- **M5 — `confirm_plan` TOCTOU race**: ~~deferred~~ → **planned as 011** in
  round 2 (conditional-UPDATE fix is unit-testable; the real-DB concurrency
  test remains optional follow-up).
- **M6 — duplicate-project race / no unique on `projects.title`**
  (`app/capture_task.py:31`): needs an Alembic migration + unique constraint +
  conflict handling. Still deferred — migration-class change. Note: plan 012
  removes the biggest duplicate *generator* (the edit loop), shrinking M6's
  practical impact.
- **M7 — broken FSM "правка" edit loop** (`confirm.py:53`, `task_router.py:272`):
  the data-pollution half (duplicate projects, orphaned proposals) is
  **planned as 012**. The UX half (multi-turn edit window, how the loop
  terminates) stays deferred pending a product decision.
- **Edit-as-replan redesign** (round 2): an edit ideally re-plans the SAME
  project instead of creating a sibling + cancelling the old one (012's
  approach). Needs a `ReplanProject` use-case; deferred as a design item.
- **add_project full single-transaction composition** (round 2): after 014,
  `create_project`/`save_plan_version`/`add_audit` are still 3 txns; a composed
  repo method would close the residual window. Deferred — small payoff after 014.
- **Solver re-schedules "done" tasks lacking fixed dates** (`greedy.py:197-206`
  pre-places done tasks only when `fixed_start and fixed_assignee_id` are set):
  LOW confidence whether any caller constructs that state today — investigate
  before planning.
- **seaborn dependency unused** (`pyproject.toml`): zero imports in `src/`
  (grep-proven). One-line removal; fold into any future deps pass.
- **Dead constant `_CAPTURE_DEFAULT_HOURS`** (`infra/db/repo.py:44`): unused;
  trivia, remove opportunistically.
- **Alembic chain** verified linear in round 2 (0001 → 25365964fe62 → 0003) —
  the untracked-0003 fork hypothesis was wrong; chain is healthy. Do note
  `0003_project_priority.py` is untracked in git — commit it.
- **M8 — no input-length cap** (`domain/models.py`, parser): add a max length on
  task title / message text. Low risk; fold into a future validation pass.
- **L1 — missing security headers** (CSP / X-Frame-Options / X-Content-Type-Options
  / HSTS): add a small middleware. Reasonable next-round DX/security item; not
  planned now to keep this round focused on authz + crashes.
- **L2 — session cookie missing `Secure`** (`routes/auth.py:18`): one-line add of
  `secure=True`, but it breaks plain-HTTP local dev unless gated on
  `not debug`. Bundle with L1 into a "web hardening" plan.
- **L3 — `edit:` callback has no admin check** (`confirm.py:47`): the actual
  write is still gated later, so it is defense-in-depth, not a live bypass.
  Low priority.
- **L4 — text "ок"/"да" confirm is a no-op** (`task_router.py:193`): only the
  inline button commits. Minor UX gap, not a defect; deferred.

### Round-3 (2026-06-15) reconcile notes — confirmed real but NOT separately planned

- **`_committed_load` double-counts re-committed projects**
  (`app/suggest_assignees.py:55-61`): sums hours across **every** committed plan
  version, and `list_committed_plans` (`repo.py`) returns all of them. Root cause
  is non-superseding committed versions — **fixed by DONE plan 012** once
  integrated. After integrating 012, verify load is counted once per project; do
  not write a separate plan. Confidence MED.
- **Load-board `pct` can exceed 100%** (`app/admin_board.py:153`): real, but the
  template renders it as plain text ("150%"), not a clamped progress bar
  (`load.html:24`), so it is informative rather than broken. Folded into plan 019
  as a documented decision (default: keep raw), not a standalone defect.
- **M8 input-length cap** (previously deferred above): the *capture* slice of it
  (blank/oversized `task_title`/`project_name` → junk rows) is now **planned as
  part of 018**. The broader parser/message-length cap remains deferred.
- **M6 duplicate-project race on capture** (`app/capture_task.py::_resolve_project`
  get-then-create with no unique on `projects.title`): still deferred —
  migration-class change (unique constraint + conflict handling). 018 reduces the
  junk-input surface but does not close the concurrency window. Unchanged from
  rounds 1–2.
- **Renderers use stateful `pyplot` global registry** (`app/render/*`): the
  exception-path *leak* is planned as 024; the larger migration to the OO
  `Figure`/`FigureCanvasAgg` API (avoids global `Gcf` state, doesn't block the
  event loop) is recorded in 024's Maintenance notes as a deferred follow-up, not
  planned now.

## Verification baseline (for reference)

The repo has a working verification story — use these, not Docker-dependent
suites, for the unit-level checks in these plans:
- Lint: `uv run ruff check src tests`
- Typecheck (domain strict per Makefile; plans use targeted strict checks):
  `uv run mypy src/planner/<path> --strict`
- Unit tests: `uv run pytest tests/unit/... -v` (no containers needed)
- Full suite incl. integration/e2e (needs Docker/testcontainers):
  `uv run pytest tests --cov=src/planner --cov-report=term-missing`
