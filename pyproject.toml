[project]
name = "planner"
version = "0.1.0"
description = "Telegram bot for auto-scheduling presales team tasks"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiogram>=3.14",
    "aiogram-dialog",
    "fastapi",
    "jinja2",
    "python-multipart",
    "sqlalchemy[asyncio]>=2.0",
    "alembic",
    "asyncpg",
    "networkx",
    "anthropic",
    "instructor",
    "openai",
    "httpx",
    "pydantic>=2.0",
    "pydantic-settings",
    "structlog",
    "apscheduler>=3.10",
    "matplotlib",
    "seaborn",
    "redis[hiredis]",
    "python-jose[cryptography]",
    "pyyaml",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "pytest-asyncio",
    "pytest-cov",
    "testcontainers[postgres,redis]",
    "vcrpy",
    "ruff",
    "mypy",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/planner"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
ignore = []

[tool.mypy]
python_version = "3.12"
strict = true
ignore_missing_imports = true

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
